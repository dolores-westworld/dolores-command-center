# Status Dashboard (Static)

Premium dark-mode dashboard scaffold made of static files only.

Constraints:
- No external libraries or assets
- No network calls
- Sample data is hardcoded in `app.js`

Files:
- `index.html` — layout + modal shell
- `styles.css` — styling + micro-interactions
- `app.js` — renders sample data into the UI
